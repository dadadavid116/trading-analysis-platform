/**
 * theme/index.ts — public entry for the design system (Phase 74).
 *
 * Import tokens and primitives from one place:
 *   import { Card, Button, Tabs, WorkspaceShell, colors, space } from '../theme';
 */

export * from './tokens';
export * from './primitives';
